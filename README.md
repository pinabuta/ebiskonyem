```
c = str(input("Вы хотите перевести Цельсия в Фаренгейты? (yes or no)\n"))
#c in f
def ira():
    uvc = int(input("Сколько градусов Цельсия у Вас показывает? - "))
    if uvc >= -273:
        temp_f = uvc * (9/5) + 32
        print (f"Вот какая температура в Фаренгейтах {temp_f}")
    else:
        print (f'Попробуйте снова, потому что \nградусы Цельсия не могут превышать "-273" \n({uvc} > -273)')
# f in c
def ura ():
    uvf = int(input("Сколько градусов в Фаренгейтах? - "))
    temp_c = (uvf - 32) * 5/9
    print (f"Вот сколько это в градусах Цельсия - {temp_c} ")
if c == "yes" :
    ira()
else: 
    ura()
```
